import React from 'react'
import SideBar from '../SideBar'
import { Box } from '@mui/system'

const index = () => {
  return (
    <SideBar>
      <Box sx={{ background: 'red' }}> Toolss</Box>
    </SideBar>
  )
}

export default index
